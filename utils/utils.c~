#include <utils/utils.h>
extern inline size_t next_pow_two(const size_t num);

extern inline void mem_swap(void *restrict x,
			    void *restrict y,
			    const size_t width);

/* if x and y might overlap */
extern inline void safe_mem_swap(void *x,
				 void *y,
				 const size_t width);
